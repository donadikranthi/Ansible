- name: conditions
  hosts: local
  connection: local
  vars:
   number: 20
  tasks:
  - name: check number is less than 10
    ansible.builtin.debug:
      msg: "Given number {{ number }} is less than 10"
    when: number < 10
  - name: check number is greater than 10
    ansible.builtin.debug:
      msg: "Given number {{ number }} is greater than 10"
    when: number > 10
  - name: check number is equal to 10
    ansible.builtin.debug:
      msg: "Given number {{ number }} is equal to 10"
    when: number == 10