- name: loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: print names
    ansible.builtin.package:
      msg: "Hello, {{ item }}"
    loop:
      - nginx 
      - mysql
      - redis
      
     